export interface MatPaginatorInterface {
    length: number;
    pageSize: number;
    pageSizeOptions: Array<number>;
}

export interface ManipulaPaginaInterface {
    pageSize: number;
    pageIndex: number;
}